# URL de base de l'API AJDCB (ajdc.api), sans slash final.
# En dev local (php artisan serve) :
VITE_API_URL=http://localhost:8000/api

# En production, pointer vers le domaine de l'API déployée, ex :
# VITE_API_URL=https://api.ajdcb.org/api
