diff --git a/src/app/pages/Contact.tsx b/src/app/pages/Contact.tsx
index 287a601..a9f9a39 100644
--- a/src/app/pages/Contact.tsx
+++ b/src/app/pages/Contact.tsx
@@ -23,7 +23,7 @@ export function Contact() {
   const onSubmit = async (data: ContactForm) => {
     setLoading(true);
     try {
-      await api.post('/messages', data);
+      await api.post('/v1/messages', data);
       toast.success('Message envoyé ! Vous recevrez une confirmation par email.');
       reset({ objet: 'question' });
     } catch (err) {
diff --git a/src/app/pages/Events.tsx b/src/app/pages/Events.tsx
index b15e0bd..4b206b8 100644
--- a/src/app/pages/Events.tsx
+++ b/src/app/pages/Events.tsx
@@ -12,7 +12,7 @@ export function Events() {
 
   useEffect(() => {
     api
-      .get<Evenement[]>('/evenements?a_venir=1')
+      .get<Evenement[]>('/v1/evenements?a_venir=1')
       .then(setEvenements)
       .catch((err) => setError(err instanceof ApiError ? err.message : 'Erreur de chargement'))
       .finally(() => setLoading(false));
@@ -92,7 +92,7 @@ export function Events() {
                       <span className="text-sm font-semibold text-red-500">Événement complet</span>
                     ) : payant ? (
                       <FedaPayButton
-                        endpoint={`/paiements/evenements/${evt.id}`}
+                        endpoint={`/v1/paiements/evenements/${evt.id}`}
                         montant={evt.prix as number}
                         devise={evt.devise || 'XOF'}
                         label="Réserver ma place"
diff --git a/src/app/pages/Join.tsx b/src/app/pages/Join.tsx
index 142b417..906b246 100644
--- a/src/app/pages/Join.tsx
+++ b/src/app/pages/Join.tsx
@@ -35,7 +35,7 @@ export function Join() {
   const onSubmit = async (data: AdhesionForm) => {
     setLoading(true);
     try {
-      await api.post('/adhesions', data);
+      await api.post('/v1/adhesions', data);
       setSubmitted(data);
       toast.success("Demande d'adhésion envoyée ! Un email de confirmation vous a été adressé.");
     } catch (err) {
@@ -226,7 +226,7 @@ export function Join() {
             </p>
             <div className="flex justify-center">
               <FedaPayButton
-                endpoint="/paiements/cotisation"
+                endpoint="/v1/paiements/cotisation"
                 extraPayload={{ mois: moisCourant }}
                 montant={1000}
                 label="Payer ma première cotisation"
diff --git a/src/app/pages/News.tsx b/src/app/pages/News.tsx
index efc7d05..0687152 100644
--- a/src/app/pages/News.tsx
+++ b/src/app/pages/News.tsx
@@ -29,7 +29,7 @@ export function News() {
 
   useEffect(() => {
     api
-      .get<Actualite[]>('/actualites')
+      .get<Actualite[]>('/v1/actualites')
       .then(setActualites)
       .catch((err) => setError(err instanceof ApiError ? err.message : 'Erreur de chargement'))
       .finally(() => setLoading(false));
